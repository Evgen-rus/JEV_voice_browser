# Copy to .env (gitignored). The key is only ever read by the Node server, never sent to the browser.
TYPESAFE_API_KEY=your_typesafe_api_key_here
# Legacy fallback name, also accepted:
# JEV_API_KEY=

# Optional
# PORT=8787
